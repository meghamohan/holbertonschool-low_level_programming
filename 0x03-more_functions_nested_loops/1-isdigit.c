/**
* _isdigit - checks if the digit is a number
* @c: int to be checked
* Return: 1 if c is uppercase, 0 otherwise
**/
int _isdigit(int c)
{
	if (c >= 48 && c <= 59)
	return (1);
	else
	return (0);
}

