#include "holberton.h"

/**
* _strlen - to find the length of a string
* @s: char pointer
* Return: length of the string
*/
int _strlen(char *s)
{
	int i;

	for (i = 1 ; s[i] != '\0' ; i++)
	{
	}
	return (i);
}
