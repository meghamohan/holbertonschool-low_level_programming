#include "holberton.h"

/**
 * main - reads elf header and prints it out.
 *
 * @argc: number of arguments
 * @argv: arguments that are passed
 * Return: Returns 1 on success, 98 on failure.
 */
int main(int argc, char *argv[])
{
	return (0);
}
